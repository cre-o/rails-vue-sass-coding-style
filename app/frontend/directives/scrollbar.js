// Template for scrollbars integration with VUE

// import jQuery from 'jquery'
// import 'scrollbar'

// export default {
//   deep: true,

//   bind () {},

//   update (options) {
//     // Destroy in case it has been initialized already.
//     // Initializing directly after destroying
//     // didn't work. Wrapping it in a setTimeout
//     // seems to do the trick.
//     setTimeout(() => {
//       jQuery(this.el).scrollbar(options)
//     }, 0)
//   },

//   unbind () {
//     jQuery(this.el).srcollbar('destroy')
//   }
// }
